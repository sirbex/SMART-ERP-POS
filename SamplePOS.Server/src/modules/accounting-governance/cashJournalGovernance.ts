/**
 * Cash / undeposited funds journal rules — placeholder for Phase 2+.
 */
import type { GovernanceJournalContext } from './types.js';

export function validateCashJournalPosting(_ctx: GovernanceJournalContext): void {
  // Phase 2: PAYMENT_RECEIPT / PAYMENT_DEPOSIT clearing invariants
}
