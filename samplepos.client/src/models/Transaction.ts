// Legacy model re-exports for compatibility with existing imports

export type { Transaction, TransactionItem as TransactionItem } from '../types';
export type { PaymentMethod } from '../types/backend';
