# Production verification credentials (Henber tenant)
# Copy to .env.proof.production and fill in TEST_PASSWORD before Step 4 smoke.
#
#   copy env.proof.production.template .env.proof.production
#   notepad .env.proof.production
#
# Load in PowerShell:
#   . .\scripts\load-proof-production-env.ps1
#
# Step 3 (AP/AR decompose — DB only):
#   npm run proof:henber:ap-decompose
#   npm run proof:henber:ar-decompose
#
# Step 4 (post-deploy smoke — DB + API):
#   npm run proof:post-deploy-smoke

# Required for Step 3 — Henber tenant Postgres (read-only proofs)
HENBER_DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@209.38.203.138:5432/pos_tenant_henber_pharmacy

# Required for Step 4 — production API smoke
BASE_URL=https://henber.wizarddigital-inv.com
CLIENT_URL=https://henber.wizarddigital-inv.com
TEST_EMAIL=admin@test.com
TEST_PASSWORD=

# Optional — browser warehouse E2E (Step 4+)
# CASHIER_EMAIL=
# CASHIER_PASSWORD=

# Release fingerprint (set after Step 1 merge)
# RELEASE_COMMIT=940a1b7cf324cd691f3890ac19c6fd06ffa0e95f
